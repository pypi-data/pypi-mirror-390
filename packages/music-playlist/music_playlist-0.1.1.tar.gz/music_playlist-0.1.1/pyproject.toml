[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "music-playlist"
version = "0.1.1"
description = "Utilities for encrypting and extracting downloaded music"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["extract_music"]
