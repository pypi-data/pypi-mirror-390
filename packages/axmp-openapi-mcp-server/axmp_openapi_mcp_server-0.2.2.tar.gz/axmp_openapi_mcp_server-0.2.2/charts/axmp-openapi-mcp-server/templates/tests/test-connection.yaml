apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "axmp-openapi-mcp-server.fullname" . }}-test-connection"
  labels:
    {{- include "axmp-openapi-mcp-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "axmp-openapi-mcp-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
