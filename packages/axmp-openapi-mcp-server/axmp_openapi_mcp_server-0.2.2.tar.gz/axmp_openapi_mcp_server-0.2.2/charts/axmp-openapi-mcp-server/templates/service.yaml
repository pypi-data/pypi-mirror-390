apiVersion: v1
kind: Service
metadata:
  name: {{ include "axmp-openapi-mcp-server.fullname" . }}
  labels:
    {{- include "axmp-openapi-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "axmp-openapi-mcp-server.selectorLabels" . | nindent 4 }}
