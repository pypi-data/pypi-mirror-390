# setup.py
from setuptools import setup  # pyright: ignore[reportMissingModuleSource]

# This file is required for backward compatibility.
# All configuration is in pyproject.toml.
if __name__ == "__main__":
    setup()
