[project]
name = "yemot"
version = "0.1.2"
description = "Python client for the Yemot (Call2All) API"
readme = "README.md"
authors = [
    { name = "davidTheDeveloper", email = "dev@code-chai.com" }
]
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
