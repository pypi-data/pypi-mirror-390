[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ytapinvl"
version = "1.1.2"
description = "CLI resmi YT API by Nauval — cek role, harga, dan beli role via QRIS"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
authors = [{name = "M. Nauval Sayyid Abdillah"}]
dependencies = ["requests>=2.31.0"]
keywords = ["ytapi", "qris", "nauval", "fastapi", "midtrans"]

[project.urls]
Homepage = "https://ytdlpyton.nvlgroup.my.id"
Repository = "https://github.com/nauvalunesa/ytapinvl"

[project.scripts]
ytapinvl = "ytapinvl.cli:main"

[tool.setuptools.packages.find]
where = ["src"]