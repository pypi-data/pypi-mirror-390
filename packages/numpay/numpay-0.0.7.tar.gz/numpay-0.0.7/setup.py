from setuptools import setup, find_packages

setup(
    name='numpay',                # the package name
    version='0.0.7',
    description='helo',
    author='aaaroo',
    author_email='aaro@gmail.com',
    packages=find_packages(),
    install_requires=[

    ],
    python_requires='>=2.1',
)

