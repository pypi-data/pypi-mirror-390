[project]
name = "shellmates"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "setuptools>=80.9.0",
    "twine>=6.2.0",
]

[project.scripts]
shellmates = "shellmates.cli:main"
shm = "shellmates.cli:main"
