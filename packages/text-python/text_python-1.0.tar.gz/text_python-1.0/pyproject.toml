[build-system]
requires = ["setuptools>=42", "wheel", "pygame>=2.6.1"]
build-backend = "setuptools.build_meta"

[project]
name = "text_python"
version = "1.0"
description = "Thu vien text_python ket hop Python, Pygame, C va C++"
authors = [
    {name = "Ung", email = "ung@example.com"}
]
dependencies = ["pygame>=2.6.1"]
dynamic = ["requires-python"]  # khai báo requires-python là dynamic
