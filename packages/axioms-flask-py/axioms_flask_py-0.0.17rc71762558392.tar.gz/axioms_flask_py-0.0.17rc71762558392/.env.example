# Flask Axioms SDK Configuration Example
# Copy this file to .env and update with your values

# Required: Your resource identifier or API audience
AXIOMS_AUDIENCE=your-api-audience-or-resource-identifier

# Option 1: Use AXIOMS_DOMAIN for Axioms or standard OAuth2/OIDC providers
# The SDK will construct the JWKS URL as: https://{AXIOMS_DOMAIN}/oauth2/.well-known/jwks.json
# Also used for namespaced claims like https://{AXIOMS_DOMAIN}/claims/roles
AXIOMS_DOMAIN=your-domain.axioms.io

# Option 2: Use AXIOMS_JWKS_URL for custom JWKS endpoints
# This takes precedence over AXIOMS_DOMAIN if both are set
# Use this if your JWKS URL doesn't follow the standard pattern
# AXIOMS_JWKS_URL=https://my-auth.domain.com/oauth2/.well-known/jwks.json

# Note: You must provide either AXIOMS_JWKS_URL or AXIOMS_DOMAIN
#
# Claims handling:
# - The SDK checks for standard claims first: 'roles', 'permissions', 'scope'
# - If AXIOMS_DOMAIN is set, it also checks namespaced claims as fallback:
#   'https://{AXIOMS_DOMAIN}/claims/roles' and 'https://{AXIOMS_DOMAIN}/claims/permissions'
