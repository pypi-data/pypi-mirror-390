name: allmark
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.7
  - pip
  # External tools required for conversion
  - pandoc
  - poppler  # provides pdftotext
  # Install the package itself via pip
  - pip:
    - -e .
