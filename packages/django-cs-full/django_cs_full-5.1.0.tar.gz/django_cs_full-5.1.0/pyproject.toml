[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "django-cs-full"        # Название пакета (уникальное на PyPI!)
version = "5.1.0"
description = "Django with extra configuration"


requires-python = ">=3.8"

authors = [{name = "Michel", email = "misheldjcus@gmail.com"}]

dependencies = [
    "django>=5.0",
]
