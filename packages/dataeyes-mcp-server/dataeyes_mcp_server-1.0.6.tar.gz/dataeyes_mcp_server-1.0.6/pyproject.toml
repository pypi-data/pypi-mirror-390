[project]
name = "dataeyes-mcp-server"
version = "1.0.6"
description = "This is a MCP server for Dataeyes"
readme = "README.md"
authors = [
    { name = "xhl", email = "xuhualun@aliyun.com" }
]
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.9.4",
]

[project.scripts]
dataeyes-mcp-server = "dataeyes_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
