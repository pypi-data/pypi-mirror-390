[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Nsc616"
version = "3.1.1"
description = "Nsc410 增强版表达式处理器"
authors = [
    {name = "王子毅"},
]
dependencies = [
]

[project.optional-dependencies]
dev = [
    "pytest>=6.0",
]

[project.scripts]
nsc410 = "Nsc616.main:main"