repos:
  - repo: local
    hooks:
      - id: lint
        name: ruff lint (via tox)
        entry: tox -q -e lint
        language: system
        pass_filenames: false
      - id: format
        name: ruff format
        entry: tox -q -e format
        language: system
        pass_filenames: false