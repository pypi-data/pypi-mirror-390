[build-system]
requires = ["setuptools>=64", "setuptools_scm>=8", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tabbiomed"
description = "TabBioMed"
readme = "README.md"
requires-python = ">=3.8"
dynamic = ["version"]
authors = [
    { name = "David Bonet", email = "davidbonetsole@gmail.com" }
]
