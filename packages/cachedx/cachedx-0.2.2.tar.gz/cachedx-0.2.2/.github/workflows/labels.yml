# Temporarily disabled - repository access issues
name: Manage Labels (Disabled)

on:
  workflow_dispatch:
    # Manually triggered only - automatic triggers disabled

# permissions:
#   issues: write
#   pull-requests: write

jobs:
  labels:
    runs-on: ubuntu-latest
    if: false  # Always skip this job
    steps:
      - name: Disabled
        run: echo "This workflow is temporarily disabled due to repository access issues"

# Original workflow (commented out):
# on:
#   push:
#     branches: [main]
#     paths:
#       - '.github/labels.yml'
#       - '.github/workflows/labels.yml'
#   workflow_dispatch:
#
# permissions:
#   issues: write
#   pull-requests: write
#
# jobs:
#   labels:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#
#       - name: Update labels
#         uses: crazy-max/ghaction-github-labeler@v5
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           yaml-file: .github/labels.yml
