name: feature request
description: something new
body:
  - type: markdown
    attributes:
      value: |
         This is for Feature Requests for the REST API

  - type: textarea
    id: freeform
    attributes:
      label: describe your actual problem
      placeholder: 'I want to do ... I tried ... It does not work because ...'
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: API Version --version
      placeholder: Version x.x.x
    validations:
      required: true