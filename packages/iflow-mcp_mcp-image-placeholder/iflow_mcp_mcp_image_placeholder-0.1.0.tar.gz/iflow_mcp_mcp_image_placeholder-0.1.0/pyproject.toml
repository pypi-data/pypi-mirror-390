[project]
name = "iflow-mcp_mcp-image-placeholder"
version = "0.1.0"
description = "MCP Image Placeholder Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["mcp[cli]>=1.2.1"]

[project.scripts]
mcp-image-placeholder = "main:mcp.run"
