{
  "wallpaper": "{{ wallpaper }}",{{ "\n" }}
{% for k, v in special.items() %}
  "{{ k }}": "{{ v }}",{% if not loop.last %}{{ "\n" }}{% endif %}
{%- endfor %}
{{ "\n" }}
{% for k, v in palette.items() %}
  "{{ k }}": "{{ v }}"{% if not loop.last %},{% endif %}{{ "\n" }}
{%- endfor %}
}