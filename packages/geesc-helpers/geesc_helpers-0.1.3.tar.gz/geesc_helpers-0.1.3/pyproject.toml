[project]
name = "geesc_helpers"
version = "0.1.3"
description = "Projeto python, com funções helpers pra baixar os arquivos do dropbox pela api"
requires-python = ">=3.13"
readme = "README.md"

[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2,<4"]

[project.urls]
Homepage = "https://github.com/geesccedeplar/geesc_helpers"