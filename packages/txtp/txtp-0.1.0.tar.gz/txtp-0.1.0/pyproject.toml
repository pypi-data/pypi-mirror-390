[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "txtp"
version = "0.1.0"
description = "A simple Text Transfer Protocol (TXTP) over TCP sockets."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Rehan Sajid Hoque", email = "idndteog19@gmail.com" }]
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/txtp"
Source = "https://github.com/txtp"
