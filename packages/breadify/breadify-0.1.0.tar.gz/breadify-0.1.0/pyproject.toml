[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "breadify"
version = "0.1.0"
description = "A dank meme library to breadify your text."
authors = [
    { name = "Venkat" }
]
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/venkatengineer/breadify"
