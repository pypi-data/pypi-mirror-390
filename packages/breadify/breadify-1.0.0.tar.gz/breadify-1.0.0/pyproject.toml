[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "breadify"
version = "1.0.0"
description = "A meme Python library that breadifies your text"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
  { name = "Venkat" }
]
requires-python = ">=3.6"
dependencies = []



[project.scripts]
breadify = "breadify.cli:main"
