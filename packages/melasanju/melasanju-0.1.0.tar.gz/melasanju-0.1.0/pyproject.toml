[build-system]
requires = ["setuptools>=61", "wheel", "build"]
build-backend = "setuptools.build_meta"

[project]
name = "melasanju"        # choose a unique name
version = "0.1.0"
description = "My 11 Python assignments packaged as a reusable library"
license = {text = "MIT"}
authors = [{name="twilight", email="twilight1234d@gmail.com"}]
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]