image: python:3.12
definitions:
  steps:
    - step: &download_execute_script
        name: Download Script Execute Script
        script:
          - echo "Retrieving Script Execution Script"
          - curl -fL https://getcli.jfrog.io | sh
          - ./jfrog rt dl --url $ARTIFACTORY_URL --user $ARTIFACTORY_USER --password $ARTIFACTORY_PASSWORD generic-packages/ci_scripts/latest/execute_script.sh
          - cat ci_scripts/latest/execute_script.sh
          - chmod 755 ci_scripts/latest/execute_script.sh
        artifacts:
          - ci_scripts/latest/execute_script.sh

    - step: &lint
        caches:
          - pip
        name: Lint
        script:
          - ./ci_scripts/latest/execute_script.sh lint_python.sh

    - step: &scan
        caches:
          - pip
        name: Scan
        script:
          - ./ci_scripts/latest/execute_script.sh scan_python.sh

    - step: &changelog
        caches:
          - pip
        name: Changelog
        script:
          - ./ci_scripts/latest/execute_script.sh check_changelog.sh

    - step: &test312
        caches:
          - pip
        name: Test Python 3.12
        script:
          - pip install -U pip
          - pip install tox
          - tox -e py312
          - ./ci_scripts/latest/execute_script.sh upload_coverage.sh

    - step: &test312-min
        caches:
          - pip
        name: Test Python 3.12 with minimum dependencies
        script:
          - pip install -U pip
          - pip install tox
          - tox -e py312-min

    - step: &test311
        image: python:3.11
        caches:
          - pip
        name: Test Python 3.11
        script:
          - pip install -U pip
          - pip install tox
          - tox -e py311

    - step: &test311-min
        image: python:3.11
        caches:
          - pip
        name: Test Python 3.11 with minimum dependencies
        script:
          - pip install -U pip
          - pip install tox
          - tox -e py311-min

    - step: &docs
        name: Test Docs
        caches:
          - pip
        script:
          - ./ci_scripts/latest/execute_script.sh test_sphinx.sh

    - step: &push
        caches:
          - pip
        name: Push
        script:
          - ./ci_scripts/latest/execute_script.sh push_python.sh

pipelines:
  default:
    - step: *download_execute_script
    - parallel:
      - step: *lint
      - step: *changelog
    - parallel:
      - step: *test312
      - step: *test312-min
      - step: *test311
      - step: *test311-min
      - step: *docs
      - step: *scan
  tags:
    'v*':
      - step: *download_execute_script
      - parallel:
          - step: *lint
          - step: *changelog
      - parallel:
          - step: *scan
          - step: *test312
          - step: *test312-min
          - step: *test311
          - step: *test311-min
          - step: *docs
      - step: *push

