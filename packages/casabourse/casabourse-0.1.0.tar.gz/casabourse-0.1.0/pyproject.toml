[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "casabourse"
version = "0.1.0"
description = "Package Python pour accéder aux données de la Bourse de Casablanca"
authors = [{ name="Koffi Frederic SESSIE", email="sessiekoffifrederic@gmail.com" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "requests",
    "beautifulsoup4",
]
[project.urls]
Homepage = "https://github.com/yourusername/casabourse"
