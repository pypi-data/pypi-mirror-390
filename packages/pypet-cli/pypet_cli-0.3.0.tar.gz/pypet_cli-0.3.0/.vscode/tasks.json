{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Install Dependencies",
            "type": "shell",
            "command": "uv venv && . .venv/bin/activate && uv pip install -e .",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
