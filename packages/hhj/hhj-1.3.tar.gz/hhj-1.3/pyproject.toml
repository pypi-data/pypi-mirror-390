[project]
name = "hhj"
version = "1.3"
description = "HHJ Engine - YAML orchestrator for automated workflows"
license = { text = "DIO - See de license" }
authors = [{ name = "NickC4p" }]
dependencies = ["pyyaml", "requests"]

[project.scripts]
hhj = "hhj.__main__:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
