[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "api-puppet"
version = "0.0.42"
authors = [
  { name="Michaël Sauget", email="michaelsauget@gmail.com" },
]
description = "A library to accelerate REST API clients development."
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    'python-dateutil',
    'requests'
]


[tool.setuptools]
packages = { find = { where = ["src"] } }
