[build-system]
requires = ["setuptools", "setuptools-scm", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 120

[tool.usort.known]
first_party = ["qpytorch"]

[tool.setuptools_scm]
local_scheme = "node-and-date"
write_to = "./qpytorch/version.py"
