[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ryomath_4"
version = "0.1.0"
description = "Demo package for PyPI publish"
authors = [{ name = "Aryan Mullick", email = "aryan.mullick@adaglobal.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.6"
