services:
  gama_vessel_base:
    build:
      cache_from:
        - type=gha
      cache_to:
        - type=gha,mode=max

  gama_vessel:
    build:
      cache_from:
        - type=gha
      cache_to:
        - type=gha,mode=max