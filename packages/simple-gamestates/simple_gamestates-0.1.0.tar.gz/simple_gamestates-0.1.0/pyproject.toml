[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
# Tell setuptools to look for a setup.cfg file for all configuration

# Pytest Yapılandırması: src klasörünü arama yoluna ekle
[tool.pytest.ini_options]
pythonpath = [
    "src"
]