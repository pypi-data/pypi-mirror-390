[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyutilix"
version = "0.1.1"
description = "A small Python utility library including Clamped and other functions."
authors = [
    {name = "M"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
