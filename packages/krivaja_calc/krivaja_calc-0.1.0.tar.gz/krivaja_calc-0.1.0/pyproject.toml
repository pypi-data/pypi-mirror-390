[project]
name = "krivaja_calc"
version = "0.1.0"
description = "it is simple calculator"
readme = "README.md"
authors = [
    { name = "bogoljub", email = "bob.stankovic@gov.ab.ca" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
krivaja-calc = "krivaja_calc:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
