services:
  missim_ue:
    volumes:
      - nas_cache:/mnt/DerivedDataCache

volumes:
  nas_cache:
    name: nas_cache
    driver_opts:
      type: "nfs"
      o: "nfsvers=4,addr=gr-nas,nolock,soft,rw"
      device: ":/volume1/unreal/DerivedDataCache"