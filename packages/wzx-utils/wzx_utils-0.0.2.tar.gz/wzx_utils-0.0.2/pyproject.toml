[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wzx_utils"                       # 包里 import 的名字，也当 PyPI 搜索名
version = "0.0.2"
description = "tool library"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "vincent", email = "zixun.wu98@outlook.com"}]
license = {text = "MIT"}
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
]

[project.urls]