[project]
name = "amde"
version = "0.1.6"
description = "Easy way to embed your data"
authors = [{ name = "amde", email = "ai.ambade00@gmail.com" }]
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
dependencies = [
  "requests"
]

[tool.setuptools.packages.find]
include = ["amde"]
