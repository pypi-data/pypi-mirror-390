[project]
name = "bt1337xearch"
version = "0.1.0"
description = "Better search for 1337x[.]to with basic filters"
readme = "README.md"
authors = [
    { name = "SimoneFelici", email = "felicisimone9@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "scrapling[fetchers]>=0.3.8",
    "textual>=6.5.0",
]

[project.scripts]
bt1337xearch = "bt1337xearch:main"

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
