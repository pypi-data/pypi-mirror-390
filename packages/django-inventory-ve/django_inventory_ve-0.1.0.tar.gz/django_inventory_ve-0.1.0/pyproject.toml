[project]
name = "django-inventory-ve"
version = "0.1.0"
description = "Inventory with warehouses, stock moves, FIFO/Avg valuation, and accounting (COGS/Asset) integration for Django."
readme = "README.md"
requires-python = ">=3.11"
authors = [{name = "Verified Enquiries / Tushar K.", email = "support@verifiedenquiries.com"}]
license = {text = "MIT"}
dependencies = [
  "Django>=5.0",
  "djangorestframework>=3.15",
  "django-finacc>=0.1.0",
]