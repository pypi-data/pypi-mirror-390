[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "g4f-utils"
version = "0.1.0"
description = ""
requires-python = ">=3.7"
dependencies = ["g4f"]

[tool.setuptools]
package-dir = {"" = "g4f_utils"}
