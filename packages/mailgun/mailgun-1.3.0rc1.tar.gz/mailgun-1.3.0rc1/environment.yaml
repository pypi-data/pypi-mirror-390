---
name: mailgun
channels:
  - defaults
dependencies:
  - python >=3.10
  # build & host deps
  - pip
  # runtime deps
  - requests >=2.32.5
  # tests
  - pytest >=7.0.0
  # other
  - pre-commit
  - toml
