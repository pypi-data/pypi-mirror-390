EQUIPMENT_INSTANCES:
  - equipment:
      adapter: HelloWorld
      data:
        instance_id: demo_device
        institute: university_lab
      requirements:
        interval: 1
  # ADD THIS - LiveView monitoring adapter
  - equipment:
      adapter: LiveViewUI
      data:
        instance_id: live_monitor
        institute: leaf
      requirements:
        monitor_topics: ["#"]  # Monitor all topics

OUTPUTS:
  - plugin: MQTT
    broker: localhost
    port: 1883
    fallback: SQL
#  - plugin: KEYDB
#    host: localhost
#    port: 6379
#    db: 0
#    fallback: FILE
#  - plugin: FILE
#    filename: local.json
#    fallback: null
  - plugin: SQL
    database: messages.db