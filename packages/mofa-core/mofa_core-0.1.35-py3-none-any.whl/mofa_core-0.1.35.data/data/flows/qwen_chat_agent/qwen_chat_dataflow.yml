nodes:
  - id: terminal-input
    build: pip install -e ../../agents/terminal-input
    path: dynamic
    outputs:
      - data
    inputs:
      agent_response: qwen_agent/llm_result

  - id: qwen_agent
    build: pip install -e ../../agents/qwen_agent
    path: qwen_agent
    outputs:
      - llm_result
    inputs:
      query: terminal-input/data
    env:
      IS_DATAFLOW_END: true
      WRITE_LOG: true

