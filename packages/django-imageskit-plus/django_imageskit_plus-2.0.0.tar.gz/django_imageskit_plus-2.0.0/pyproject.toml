[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "django-imageskit-plus"
version = "2.0.0"
description = "django-imageskit"
authors = [
    {name = "viruss", email = "viruss@email.com"}
]
dependencies = [
    "Django>=4.0",
    "Pillow>=9.0"
]