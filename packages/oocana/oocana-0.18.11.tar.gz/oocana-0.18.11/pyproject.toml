[project]
name = "oocana"
version = "0.18.11"
description = "python implement of oocana to give a context for oocana block"
dependencies = [
    "paho-mqtt>=2",
    "simplejson>=3.19.2",
]
requires-python = ">=3.9"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
