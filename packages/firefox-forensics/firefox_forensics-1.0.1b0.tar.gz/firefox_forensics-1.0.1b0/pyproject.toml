[build-system]
requires = ["setuptools>=70.0", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "firefox-forensics"
dynamic = ["version", "scripts", "readme", "description", "license", "classifiers", "authors"]
dependencies = [
  "setuptools>=70"
]
