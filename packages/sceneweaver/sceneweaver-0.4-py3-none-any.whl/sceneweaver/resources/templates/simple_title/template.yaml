- type: svg
  id: title
  template: title_only.svg
  duration: "{{ duration | default(3) }}"
  params:
    title: "{{ title | default('Default Title') }}"
    # Pass the font from the context down to the SVG.
    font_family: "{{ font }}"
