[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "f4compete"
version = "0.1.0"
description = ""
requires-python = ">=3.7"
dependencies = ["g4f"]

[tool.setuptools]
package-dir = {"" = "f4compete"}
