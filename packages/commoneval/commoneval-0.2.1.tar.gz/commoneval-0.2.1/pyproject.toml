[project]
name = "commoneval"
version = "0.2.1"
description = ""
authors = [
    {name = "Sean Boisen",email = "sean.boisen@biblica.com"}
]
repository = "https://github.com/Clear-Bible/CommonEval"
readme = "README.md"
packages = [
  {include = "commoneval"}
]

[tool.poetry.dependencies]
Python = ">=3.10"
jsonlines = "^4.0.0"
pyyaml = "^6.0.2"
# these are downgraded for compatability: not sure what the proper values
# ought to be
mkdocs = "^1.3.0"
mkdocs-material = "^8.3.8"
mkdocstrings = "^0.18.1"
unicodecsv = "^0.14.1"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"
pytest-cov = "^6.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

