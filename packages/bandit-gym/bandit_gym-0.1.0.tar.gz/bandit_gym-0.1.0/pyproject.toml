[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bandit-gym"
version = "0.1.0"
authors = [
  { name="Rishi Vora", email="Vorarishi04@gmail.com" }
]
description = "A Gym-compatible Multi-Armed Bandit simulator."
readme = "README.md"
license = { text = "MIT License" }
requires-python = ">=3.7"
dependencies = ["gym", "numpy"]