[build-system]
requires = [
    "setuptools", "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "credipronto"
version = "0.2.1"
description = "Biblioteca de utilidades time de dados"
readme = "README.md"
license = { text = "MIT" }
authors = [
  { name = "Alexandre Vieira", email = "avsilva@credipronto.com.br" }
]
dependencies = [
    "elastic-apm>=6.24.0",
    "dotenv>=0.9.9"
]

[project.urls]

