[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "selfw"
version = "0.2.7"
authors = [
  { name="Iván", email="ggivixd@gmail.com" },
]
description = "Un framework Selenium para automatizar pruebas de forma simple"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
dependencies = [
  "selenium>=4.0.0",
  "pytest"
]

[project.urls]
Homepage = "https://pypi.org/user/ivanbp/"