[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "FrozenJson"
version = "0.0.3"
description = "使用属性表示法浏览类似JSON的对象的只读外观。"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
  { name="tongqing", email="toneching@163.com" }
]
dependencies = [
]

[project.urls]
Homepage = "https://github.com/TongTsing/fluent-Python"
