# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper
from payrix.configuration import Server
from payrix.controllers.base_controller import BaseController
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter
from payrix.http.http_method_enum import HttpMethodEnum
from apimatic_core.authentication.multiple.single_auth import Single
from apimatic_core.authentication.multiple.or_auth_group import Or
from payrix.models.logins_response_result import LoginsResponseResult
from payrix.exceptions.error_four_hundred_exception import ErrorFourHundredException


class LoginsController(BaseController):

    """A Controller to access Endpoints in the payrix API."""
    def __init__(self, config):
        super(LoginsController, self).__init__(config)

    def get_logins_id(self,
                      id,
                      token=None,
                      request_token=None):
        """Does a GET request to /logins/{id}.

        Query a Login. A Login is a user of the API and has a unique username
        and password.

        Args:
            id (str): The ID of this resource and The Login ID.
            token (str, optional): A single-use, limited authentication layer
                in addition to an API Key or Session Key to authorize
                sensitive operations. <a
                href="/apis/payrix/dev-int-guide/references/authentication/auth
                -tokens" target="_blank">Auth Tokens</a> add security by
                requiring one-time approval for specific actions.
            request_token (str, optional): A custom, one-time identifier for
                any API request (GET, PUT, POST, or DELETE). Blocks future
                requests with the same token for 48 hours, ensuring only the
                first request is processed. Valid values can contain 1-100
                alphanumeric and special characters. See <a
                href="/apis/payrix/dev-int-guide/references/request-tokens"
                target="_blank">Request Tokens</a>  for more information.

        Returns:
            LoginsResponseResult: Response from the API. Retrieve the Response
                for the query on Logins resource.

        Raises:
            APIException: When an error occurs while fetching the data from
                the remote API. This exception includes the HTTP Response
                code, an error message, and the HTTP body that was received in
                the request.

        """

        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.PAYRIX)
            .path('/logins/{id}')
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                            .key('id')
                            .value(id)
                            .should_encode(True))
            .header_param(Parameter()
                          .key('token')
                          .value(token))
            .header_param(Parameter()
                          .key('request-token')
                          .value(request_token))
            .header_param(Parameter()
                          .key('accept')
                          .value('application/json'))
            .auth(Or(Single('apiKey'), Single('sessionKey'), Single('txnSessionKey')))
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(LoginsResponseResult.from_dictionary)
            .local_error('400', 'Error', ErrorFourHundredException)
        ).execute()

    def put_logins_id(self,
                      id,
                      body,
                      token=None,
                      request_token=None):
        """Does a PUT request to /logins/{id}.

        Update a Login, which is a user of the API having a unique username
        and password.

        Args:
            id (str): The ID of this resource and The Login ID.
            body (LoginsPutRequest): Update Login Request
            token (str, optional): A single-use, limited authentication layer
                in addition to an API Key or Session Key to authorize
                sensitive operations. <a
                href="/apis/payrix/dev-int-guide/references/authentication/auth
                -tokens" target="_blank">Auth Tokens</a> add security by
                requiring one-time approval for specific actions.
            request_token (str, optional): A custom, one-time identifier for
                any API request (GET, PUT, POST, or DELETE). Blocks future
                requests with the same token for 48 hours, ensuring only the
                first request is processed. Valid values can contain 1-100
                alphanumeric and special characters. See <a
                href="/apis/payrix/dev-int-guide/references/request-tokens"
                target="_blank">Request Tokens</a>  for more information.

        Returns:
            LoginsResponseResult: Response from the API. Response for Update
                Login Response

        Raises:
            APIException: When an error occurs while fetching the data from
                the remote API. This exception includes the HTTP Response
                code, an error message, and the HTTP body that was received in
                the request.

        """

        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.PAYRIX)
            .path('/logins/{id}')
            .http_method(HttpMethodEnum.PUT)
            .template_param(Parameter()
                            .key('id')
                            .value(id)
                            .should_encode(True))
            .header_param(Parameter()
                          .key('Content-Type')
                          .value('application/json'))
            .body_param(Parameter()
                        .value(body))
            .header_param(Parameter()
                          .key('token')
                          .value(token))
            .header_param(Parameter()
                          .key('request-token')
                          .value(request_token))
            .header_param(Parameter()
                          .key('accept')
                          .value('application/json'))
            .body_serializer(APIHelper.json_serialize)
            .auth(Or(Single('apiKey'), Single('sessionKey'), Single('txnSessionKey')))
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(LoginsResponseResult.from_dictionary)
            .local_error('400', 'Error', ErrorFourHundredException)
        ).execute()

    def delete_logins_id(self,
                         id,
                         token=None,
                         request_token=None):
        """Does a DELETE request to /logins/{id}.

        Delete a Login. A Login is a user of the API and has a unique username
        and password.

        Args:
            id (str): The ID of this resource and The Login ID.
            token (str, optional): A single-use, limited authentication layer
                in addition to an API Key or Session Key to authorize
                sensitive operations. <a
                href="/apis/payrix/dev-int-guide/references/authentication/auth
                -tokens" target="_blank">Auth Tokens</a> add security by
                requiring one-time approval for specific actions.
            request_token (str, optional): A custom, one-time identifier for
                any API request (GET, PUT, POST, or DELETE). Blocks future
                requests with the same token for 48 hours, ensuring only the
                first request is processed. Valid values can contain 1-100
                alphanumeric and special characters. See <a
                href="/apis/payrix/dev-int-guide/references/request-tokens"
                target="_blank">Request Tokens</a>  for more information.

        Returns:
            LoginsResponseResult: Response from the API. Response for Delete
                Login Response

        Raises:
            APIException: When an error occurs while fetching the data from
                the remote API. This exception includes the HTTP Response
                code, an error message, and the HTTP body that was received in
                the request.

        """

        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.PAYRIX)
            .path('/logins/{id}')
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                            .key('id')
                            .value(id)
                            .should_encode(True))
            .header_param(Parameter()
                          .key('token')
                          .value(token))
            .header_param(Parameter()
                          .key('request-token')
                          .value(request_token))
            .header_param(Parameter()
                          .key('accept')
                          .value('application/json'))
            .auth(Or(Single('apiKey'), Single('sessionKey'), Single('txnSessionKey')))
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(LoginsResponseResult.from_dictionary)
            .local_error('400', 'Error', ErrorFourHundredException)
        ).execute()

    def get_logins(self,
                   token=None,
                   request_token=None,
                   search=None,
                   totals=None,
                   page_number=None,
                   page_limit=None):
        """Does a GET request to /logins.

        Query Logins. Retrieves the list of all users details like name and
        email as well as management details like which partition and division
        the user belongs to. A Login represents the unique ID of the user
        making an API request. Each user has a unique username and password

        Args:
            token (str, optional): A single-use, limited authentication layer
                in addition to an API Key or Session Key to authorize
                sensitive operations. <a
                href="/apis/payrix/dev-int-guide/references/authentication/auth
                -tokens" target="_blank">Auth Tokens</a> add security by
                requiring one-time approval for specific actions.
            request_token (str, optional): A custom, one-time identifier for
                any API request (GET, PUT, POST, or DELETE). Blocks future
                requests with the same token for 48 hours, ensuring only the
                first request is processed. Valid values can contain 1-100
                alphanumeric and special characters. See <a
                href="/apis/payrix/dev-int-guide/references/request-tokens"
                target="_blank">Request Tokens</a>  for more information.
            search (str, optional): Set this header to filter or sort the list
                of resources that the method returns.  See
                [Searches](page:welcome#searches) for detailed information and
                examples on how to use search header.
            totals (str, optional): To configure a request to return a total
                for all instances of a field in a result set,  use the totals
                header in the format `totals={operator}[]={key}`.  This will
                calculate the desired total and return it in the `details >
                totals` object of the response.  For instance, if you want to
                sum the `total` field of all transactions,  you would use the
                `sum` operator. The response will include the result set, 
                along with the calculated total in the `details` section. See
                [Collection Operators](page:welcome#collection-operators) for
                detailed information and examples on how to use totals header.
            page_number (int, optional): Set this path parameter to request a
                specific page of records.  For example, set `?page[number]=2`
                to retrieve the second page of records for this request.
            page_limit (int, optional): Set this path parameter to request up
                to a specific amount of records. By default 30 records are
                retrieved per page. The maximum limit that can be set is 100.
                For example, set `?page[limit]=50` to retrieve up to 50
                records for this request.

        Returns:
            LoginsResponseResult: Response from the API. Retrieve Logins
                Response for query on logins resource.

        Raises:
            APIException: When an error occurs while fetching the data from
                the remote API. This exception includes the HTTP Response
                code, an error message, and the HTTP body that was received in
                the request.

        """

        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.PAYRIX)
            .path('/logins')
            .http_method(HttpMethodEnum.GET)
            .header_param(Parameter()
                          .key('token')
                          .value(token))
            .header_param(Parameter()
                          .key('request-token')
                          .value(request_token))
            .header_param(Parameter()
                          .key('search')
                          .value(search))
            .header_param(Parameter()
                          .key('totals')
                          .value(totals))
            .query_param(Parameter()
                         .key('page[number]')
                         .value(page_number))
            .query_param(Parameter()
                         .key('page[limit]')
                         .value(page_limit))
            .header_param(Parameter()
                          .key('accept')
                          .value('application/json'))
            .auth(Or(Single('apiKey'), Single('sessionKey'), Single('txnSessionKey')))
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(LoginsResponseResult.from_dictionary)
            .local_error('400', 'Error', ErrorFourHundredException)
        ).execute()

    def post_logins(self,
                    body,
                    token=None,
                    request_token=None):
        """Does a POST request to /logins.

        Create a Login. A Login represents the unique ID of the user making an
        API request. Each user has a unique username and password.

        Args:
            body (LoginsPostRequest): Create Login Request
            token (str, optional): A single-use, limited authentication layer
                in addition to an API Key or Session Key to authorize
                sensitive operations. <a
                href="/apis/payrix/dev-int-guide/references/authentication/auth
                -tokens" target="_blank">Auth Tokens</a> add security by
                requiring one-time approval for specific actions.
            request_token (str, optional): A custom, one-time identifier for
                any API request (GET, PUT, POST, or DELETE). Blocks future
                requests with the same token for 48 hours, ensuring only the
                first request is processed. Valid values can contain 1-100
                alphanumeric and special characters. See <a
                href="/apis/payrix/dev-int-guide/references/request-tokens"
                target="_blank">Request Tokens</a>  for more information.

        Returns:
            LoginsResponseResult: Response from the API. Response for create
                on logins resource

        Raises:
            APIException: When an error occurs while fetching the data from
                the remote API. This exception includes the HTTP Response
                code, an error message, and the HTTP body that was received in
                the request.

        """

        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.PAYRIX)
            .path('/logins')
            .http_method(HttpMethodEnum.POST)
            .header_param(Parameter()
                          .key('Content-Type')
                          .value('application/json'))
            .body_param(Parameter()
                        .value(body))
            .header_param(Parameter()
                          .key('token')
                          .value(token))
            .header_param(Parameter()
                          .key('request-token')
                          .value(request_token))
            .header_param(Parameter()
                          .key('accept')
                          .value('application/json'))
            .body_serializer(APIHelper.json_serialize)
            .auth(Or(Single('apiKey'), Single('sessionKey'), Single('txnSessionKey')))
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(LoginsResponseResult.from_dictionary)
            .local_error('400', 'Error', ErrorFourHundredException)
        ).execute()
