# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""

from enum import Enum
from apimatic_core.http.configurations.http_client_configuration import HttpClientConfiguration
from apimatic_requests_client_adapter.requests_client import RequestsClient


class Environment(Enum):
    """An enum for SDK environments"""
    SANDBOX = 0
    CANADA_SANDBOX = 1


class Server(Enum):
    """An enum for API servers"""
    PAYRIX = 0


class Configuration(HttpClientConfiguration):
    """A class used for configuring the SDK by a user.
    """

    @property
    def environment(self):
        return self._environment

    @property
    def api_key_credentials(self):
        return self._api_key_credentials

    @property
    def session_key_credentials(self):
        return self._session_key_credentials

    @property
    def txn_session_key_credentials(self):
        return self._txn_session_key_credentials

    @property
    def username_credentials(self):
        return self._username_credentials

    @property
    def password_credentials(self):
        return self._password_credentials

    def __init__(self, http_client_instance=None,
                 override_http_client_configuration=False, http_call_back=None,
                 timeout=60, max_retries=0, backoff_factor=2,
                 retry_statuses=None, retry_methods=None, proxy_settings=None,
                 environment=Environment.SANDBOX, api_key_credentials=None,
                 session_key_credentials=None, txn_session_key_credentials=None,
                 username_credentials=None, password_credentials=None):
        if retry_methods is None:
            retry_methods = ['GET', 'PUT']

        if retry_statuses is None:
            retry_statuses = [408, 413, 429, 500, 502, 503, 504, 521, 522, 524]

        super().__init__(
            http_client_instance=http_client_instance,
            override_http_client_configuration=override_http_client_configuration,
            http_call_back=http_call_back, timeout=timeout,
            max_retries=max_retries, backoff_factor=backoff_factor,
            retry_statuses=retry_statuses, retry_methods=retry_methods,
            proxy_settings=proxy_settings
        )

        # Current API environment
        self._environment = environment

        # The object holding Custom Header Signature credentials
        self._api_key_credentials = api_key_credentials

        # The object holding Custom Header Signature credentials
        self._session_key_credentials = session_key_credentials

        # The object holding Custom Header Signature credentials
        self._txn_session_key_credentials = txn_session_key_credentials

        # The object holding Custom Header Signature credentials
        self._username_credentials = username_credentials

        # The object holding Custom Header Signature credentials
        self._password_credentials = password_credentials

        # The Http Client to use for making requests.
        self.set_http_client(self.create_http_client())

    def clone_with(self, http_client_instance=None,
                   override_http_client_configuration=None, http_call_back=None,
                   timeout=None, max_retries=None, backoff_factor=None,
                   retry_statuses=None, retry_methods=None, proxy_settings=None,
                   environment=None, api_key_credentials=None,
                   session_key_credentials=None,
                   txn_session_key_credentials=None, username_credentials=None,
                   password_credentials=None):
        http_client_instance = http_client_instance or self.http_client_instance
        override_http_client_configuration = override_http_client_configuration or self.override_http_client_configuration
        http_call_back = http_call_back or self.http_callback
        timeout = timeout or self.timeout
        max_retries = max_retries or self.max_retries
        backoff_factor = backoff_factor or self.backoff_factor
        retry_statuses = retry_statuses or self.retry_statuses
        retry_methods = retry_methods or self.retry_methods
        proxy_settings = proxy_settings or self.proxy_settings
        environment = environment or self.environment
        api_key_credentials = api_key_credentials or self.api_key_credentials
        session_key_credentials = session_key_credentials or self.session_key_credentials
        txn_session_key_credentials = txn_session_key_credentials or self.txn_session_key_credentials
        username_credentials = username_credentials or self.username_credentials
        password_credentials = password_credentials or self.password_credentials
        return Configuration(
            http_client_instance=http_client_instance,
            override_http_client_configuration=override_http_client_configuration,
            http_call_back=http_call_back, timeout=timeout,
            max_retries=max_retries, backoff_factor=backoff_factor,
            retry_statuses=retry_statuses, retry_methods=retry_methods,
            proxy_settings=proxy_settings, environment=environment,
            api_key_credentials=api_key_credentials,
            session_key_credentials=session_key_credentials,
            txn_session_key_credentials=txn_session_key_credentials,
            username_credentials=username_credentials,
            password_credentials=password_credentials
        )

    def create_http_client(self):
        return RequestsClient(
            timeout=self.timeout, max_retries=self.max_retries,
            backoff_factor=self.backoff_factor, retry_statuses=self.retry_statuses,
            retry_methods=self.retry_methods,
            http_client_instance=self.http_client_instance,
            override_http_client_configuration=self.override_http_client_configuration,
            response_factory=self.http_response_factory,
            proxies=self.proxy_settings.to_proxies() if self.proxy_settings else None
        )

    # All the environments the SDK can run in
    environments = {
        Environment.SANDBOX: {
            Server.PAYRIX: 'https://test-api.payrix.com'
        },
        Environment.CANADA_SANDBOX: {
            Server.PAYRIX: 'https://test-api.payrixcanada.com'
        }
    }

    def get_base_uri(self, server=Server.PAYRIX):
        """Generates the appropriate base URI for the environment and the
        server.

        Args:
            server (Configuration.Server): The server enum for which the base
            URI is required.

        Returns:
            String: The base URI.

        """
        return self.environments[self.environment][server]
