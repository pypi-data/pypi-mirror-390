# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from payrix.api_helper import APIHelper
from payrix.models.messages_read_enum import MessagesReadEnum


class MessagesPostRequest(object):

    """Implementation of the 'messagesPostRequest' model.

    Attributes:
        message_thread (str): The identifier of the messageThreads that owns
            this Messages resource.
        secure (SecureEnum): Whether this resource is marked as secure. 
            <details> <summary>Valid Values</summary>  - `0` - **Messages will
            protected in email notifications.**   - `1` - **Message will
            display entirely.**  </details>
        read (MessagesReadEnum): Whether this resource is marked as read. 
            <details> <summary>Valid Values</summary>  - `0` - **Message has
            not been read yet.**  - `1` - **Message has been read and a
            value.**   </details>
        message (str): Free-form text for adding a message to a messageThread
            resource.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "message_thread": 'messageThread',
        "read": 'read',
        "message": 'message',
        "secure": 'secure'
    }

    _optionals = [
        'secure',
    ]

    def __init__(self,
                 message_thread=None,
                 read=None,
                 message=None,
                 secure=0):
        """Constructor for the MessagesPostRequest class"""

        # Initialize members of the class
        self.message_thread = message_thread 
        self.secure = secure 
        self.read = read 
        self.message = message 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        message_thread = dictionary.get("messageThread") if dictionary.get("messageThread") else None
        read = dictionary.get("read") if dictionary.get("read") else None
        message = dictionary.get("message") if dictionary.get("message") else None
        secure = dictionary.get("secure") if dictionary.get("secure") else 0
        # Return an object of this model
        return cls(message_thread,
                   read,
                   message,
                   secure)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.message_thread,
                                           type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.read,
                                            type_callable=lambda value: MessagesReadEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.message,
                                            type_callable=lambda value: isinstance(value, str))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('messageThread'),
                                       type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('read'),
                                        type_callable=lambda value: MessagesReadEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('message'),
                                        type_callable=lambda value: isinstance(value, str))

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'message_thread={self.message_thread!r}, '
                f'secure={(self.secure if hasattr(self, "secure") else None)!r}, '
                f'read={self.read!r}, '
                f'message={self.message!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'message_thread={self.message_thread!s}, '
                f'secure={(self.secure if hasattr(self, "secure") else None)!s}, '
                f'read={self.read!s}, '
                f'message={self.message!s})')
