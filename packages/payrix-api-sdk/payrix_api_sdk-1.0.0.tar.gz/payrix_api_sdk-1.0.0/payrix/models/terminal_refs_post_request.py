# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from payrix.api_helper import APIHelper
from payrix.models.processor_enum import ProcessorEnum
from payrix.models.terminal_ref_platform_enum import TerminalRefPlatformEnum
from payrix.models.terminal_ref_stage_1_enum import TerminalRefStage1Enum


class TerminalRefsPostRequest(object):

    """Implementation of the 'terminalRefsPostRequest' model.

    Attributes:
        terminal (str): The identifier of the Terminal that owns this
            terminalRefs resource.
        ref (str): The reference code itself. This field is stored as a text
            string and must be between 0 and 50 characters long.
        stage (TerminalRefStage1Enum): An indicator showing what this
            terminalRef refers to, such as 'create', 'pid', 'tid', 'token', or
            'ctid'.  <details> <summary>Valid Values</summary>  - `create`  - 
            **Terminal Creation** - `pid`  -  **Platform ID** - `tid`  - 
            **Terminal ID** - `token`  -  **Token ID** - `ctid`  -  **Custom
            Terminal ID**  </details>
        platform (TerminalRefPlatformEnum): The platform that issued this
            terminalRef.  <details> <summary>Valid Values</summary>  -
            `ELAVON` - **ELAVON Payments Platform.** - `FIRSTDATA` -
            **FirstData Payments Platform.** - `VCORE` - **WorldPay / Vantiv
            Core Payments Platform.** - `VANTIV` - **WorldPay / Vantiv eComm
            Payments (VAP) Platform.** - `WELLSFARGO` - **Wells Fargo Merchant
            Services Payments Platform.** - `SOUNDPAYMENTS` - **SoundPayments
            EMV and POS Payments Platform.** - `PAYRIX` - **Payrix Merchant
            Payments Platform.**  </details>
        processor (ProcessorEnum): The processor that issued this
            terminalTxnRef.  <details> <summary>Valid Values</summary>  -
            `APPLE` - **APPLE** - `ELAVON` - **ELAVON** - `FIRSTDATA` -
            **FIRSTDATA** - `GOOGLE` - **GOOGLE** - `VANTIV` - **VANTIV** -
            `VCORE` - **VCORE** - `WELLSACH` - **WELLSACH** - `WELLSFARGO` -
            **WELLSFARGO** - `WFSINGLE` - **WFSINGLE** - `WORLDPAY` -
            **WORLDPAY** - `TDBANKCA` - **TDBANKCA**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "terminal": 'terminal',
        "ref": 'ref',
        "stage": 'stage',
        "platform": 'platform',
        "processor": 'processor'
    }

    def __init__(self,
                 terminal=None,
                 ref=None,
                 stage=None,
                 platform=None,
                 processor=None):
        """Constructor for the TerminalRefsPostRequest class"""

        # Initialize members of the class
        self.terminal = terminal 
        self.ref = ref 
        self.stage = stage 
        self.platform = platform 
        self.processor = processor 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        terminal = dictionary.get("terminal") if dictionary.get("terminal") else None
        ref = dictionary.get("ref") if dictionary.get("ref") else None
        stage = dictionary.get("stage") if dictionary.get("stage") else None
        platform = dictionary.get("platform") if dictionary.get("platform") else None
        processor = dictionary.get("processor") if dictionary.get("processor") else None
        # Return an object of this model
        return cls(terminal,
                   ref,
                   stage,
                   platform,
                   processor)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.terminal,
                                           type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.ref,
                                            type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.stage,
                                            type_callable=lambda value: TerminalRefStage1Enum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.platform,
                                            type_callable=lambda value: TerminalRefPlatformEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.processor,
                                            type_callable=lambda value: ProcessorEnum.validate(value))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('terminal'),
                                       type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('ref'),
                                        type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('stage'),
                                        type_callable=lambda value: TerminalRefStage1Enum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('platform'),
                                        type_callable=lambda value: TerminalRefPlatformEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('processor'),
                                        type_callable=lambda value: ProcessorEnum.validate(value))

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'terminal={self.terminal!r}, '
                f'ref={self.ref!r}, '
                f'stage={self.stage!r}, '
                f'platform={self.platform!r}, '
                f'processor={self.processor!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'terminal={self.terminal!s}, '
                f'ref={self.ref!s}, '
                f'stage={self.stage!s}, '
                f'platform={self.platform!s}, '
                f'processor={self.processor!s})')
