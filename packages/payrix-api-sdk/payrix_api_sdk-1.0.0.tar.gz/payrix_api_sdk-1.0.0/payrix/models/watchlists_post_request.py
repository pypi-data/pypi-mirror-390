# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from payrix.api_helper import APIHelper
from payrix.models.default_enum import DefaultEnum
from payrix.models.frozen_enum import FrozenEnum
from payrix.models.inactive_enum import InactiveEnum


class WatchlistsPostRequest(object):

    """Implementation of the 'watchlistsPostRequest' model.

    Attributes:
        login (str): The identifier of the Login associated with this
            Watchlist.
        integration (WatchlistIntegrationEnum): The integration related to
            this Watchlist.  <details> <summary>Valid Values</summary>  -
            `APPLE` - **APPLE integration.** - `ELAVON` - **ELAVON
            integration.** - `FEDACH` - **FEDACH integration.** - `FIRSTDATA`
            - **FIRSTDATA integration.** - `NEUTRINO` - **NEUTRINO
            integration.** - `OFAC` - **OFAC integration.** - `PAYRIX` -
            **PAYRIX integration.** - `PLAID` - **PLAID integration.** -
            `SIFT` - **SIFT integration.** - `SOCURE` - **SOCURE
            integration.** - `SOUNDPAYMENTS` - **SOUNDPAYMENTS integration.**
            - `TDBANK` - **TDBANK integration.** - `TDBANKCA` - **TDBANKCA
            integration.** - `VANTIV` - **VANTIV integration.** - `VCORE` -
            **VCORE integration.** - `WEBSHIELD` - **WEBSHIELD integration.**
            - `WELLSACH` - **WELLSACH integration.** - `WELLSFARGO` -
            **WELLSFARGO integration.** - `WFSINGLE` - **WFSINGLE
            integration.** - `WORLDPAY` - **WORLDPAY integration.**  </details>
        name (str): The name of this Watchlist.  This field is stored as a
            text string and must be between 0 and 500 characters long.
        description (str): A description of this Watchlist.  This field is
            stored as a text string and must be between 0 and 500 characters
            long.
        default (DefaultEnum): Whether this Watchlist applies to everyone. 
            <details> <summary>Valid Values</summary>  - `0` - **Non-Default**
            - `1` - **Default**  </details>
        custom (str): A custom identifier for this Watchlist.  This field is
            stored as a text string and must be between 0 and 500 characters
            long.
        inactive (InactiveEnum): Whether this resource is marked as inactive. 
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen. 
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "login": 'login',
        "default": 'default',
        "inactive": 'inactive',
        "frozen": 'frozen',
        "integration": 'integration',
        "name": 'name',
        "description": 'description',
        "custom": 'custom'
    }

    _optionals = [
        'integration',
        'name',
        'description',
        'custom',
    ]

    def __init__(self,
                 login=None,
                 default=0,
                 inactive=0,
                 frozen=0,
                 integration=APIHelper.SKIP,
                 name=APIHelper.SKIP,
                 description=APIHelper.SKIP,
                 custom=APIHelper.SKIP):
        """Constructor for the WatchlistsPostRequest class"""

        # Initialize members of the class
        self.login = login 
        if integration is not APIHelper.SKIP:
            self.integration = integration 
        if name is not APIHelper.SKIP:
            self.name = name 
        if description is not APIHelper.SKIP:
            self.description = description 
        self.default = default 
        if custom is not APIHelper.SKIP:
            self.custom = custom 
        self.inactive = inactive 
        self.frozen = frozen 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        login = dictionary.get("login") if dictionary.get("login") else None
        default = dictionary.get("default") if dictionary.get("default") else 0
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        integration = dictionary.get("integration") if dictionary.get("integration") else APIHelper.SKIP
        name = dictionary.get("name") if dictionary.get("name") else APIHelper.SKIP
        description = dictionary.get("description") if dictionary.get("description") else APIHelper.SKIP
        custom = dictionary.get("custom") if dictionary.get("custom") else APIHelper.SKIP
        # Return an object of this model
        return cls(login,
                   default,
                   inactive,
                   frozen,
                   integration,
                   name,
                   description,
                   custom)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.login,
                                           type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.default,
                                            type_callable=lambda value: DefaultEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.inactive,
                                            type_callable=lambda value: InactiveEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.frozen,
                                            type_callable=lambda value: FrozenEnum.validate(value))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('login'),
                                       type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('default'),
                                        type_callable=lambda value: DefaultEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('inactive'),
                                        type_callable=lambda value: InactiveEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('frozen'),
                                        type_callable=lambda value: FrozenEnum.validate(value))

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'login={self.login!r}, '
                f'integration={(self.integration if hasattr(self, "integration") else None)!r}, '
                f'name={(self.name if hasattr(self, "name") else None)!r}, '
                f'description={(self.description if hasattr(self, "description") else None)!r}, '
                f'default={self.default!r}, '
                f'custom={(self.custom if hasattr(self, "custom") else None)!r}, '
                f'inactive={self.inactive!r}, '
                f'frozen={self.frozen!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'login={self.login!s}, '
                f'integration={(self.integration if hasattr(self, "integration") else None)!s}, '
                f'name={(self.name if hasattr(self, "name") else None)!s}, '
                f'description={(self.description if hasattr(self, "description") else None)!s}, '
                f'default={self.default!s}, '
                f'custom={(self.custom if hasattr(self, "custom") else None)!s}, '
                f'inactive={self.inactive!s}, '
                f'frozen={self.frozen!s})')
