# When pytest runs, it automatically detects and uses conftest.py files for configuration.
# Placing a conftest.py file in the root of your project can help ensure that the root directory is included in
# the Python path, making it easier to import your modules without running into ModuleNotFoundError
