# Changelog

## 0.1.0 – First release

Today we’re happily shipping `headless-coder-sdk-codex`, the Python adapter that wraps the Codex CLI with the shared headless coder interface. It brings the same thread lifecycle and streaming semantics from the TypeScript repo directly into Python.
