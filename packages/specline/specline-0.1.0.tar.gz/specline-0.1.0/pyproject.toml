[project]
name = "specline"
version = "0.1.0"
description = "SpecLine CLI for managing specline/ OpenSpec workflows"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8.0"]

[project.scripts]
specline = "spec_line.cli:main"
palprompt = "spec_line.cli:main"
