# Generated by vovk-cli v0.0.1-draft.399 at 2025-11-07T10:09:56.113Z

[build-system]
requires = [ "hatchling" ]
build-backend = "hatchling.build"

[project]
name = "vovk_hello_world"
version = "0.0.62"
description = "A showcase for Next.js + Vovk.ts + Zod, demonstrating its capabilities with TypeScript, Rust, and Python RPC."
requires-python = ">=3.8"
authors = [ ]
dependencies = [ "requests", "jsonschema", "rfc3987", "urllib3==1.26.15" ]
readme = "README.md"

  [project.license]
  text = "MIT"

  [project.optional-dependencies]
  dev = [ "types-requests", "types-jsonschema" ]


[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["py.typed"]

[tool.mypy]
python_version = "3.8"
warn_return_any = true
warn_unused_configs = true