[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "xmagic-sdk"
version = "0.0.1"
description = "xmagic-sdk"
readme = "README.md"
requires-python = ">=3.11"
