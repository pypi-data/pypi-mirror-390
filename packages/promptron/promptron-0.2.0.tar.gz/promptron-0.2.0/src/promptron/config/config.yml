# Promptron Configuration
# Each entry defines: category (one of 5 recommended), topic (user-defined), and count of questions

prompts:
  - category: "default"
    topic: "openshift"
    count: 5
  
  - category: "red_teaming"
    topic: "kubernetes"
    count: 3
