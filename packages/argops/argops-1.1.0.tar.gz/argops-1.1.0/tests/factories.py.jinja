"""Define the factories of the program."""

from typing import Any

from pydantic_factories import ModelFactory

from {{ name | slugify }} import model


# class ModelFactory(ModelFactory[Any]):
#     """Define factory for the Model model."""
# 
#     __model__ = model.
