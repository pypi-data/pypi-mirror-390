name: base
channels:
  - conda-forge
dependencies:
  - python = 3.11
  - arrakis-python >= 0.2
  - appdirs
  - gpstime
  - numpy
  - pyarrow-all  # needed for arrow flight
  - python-confluent-kafka
  - pip
  - pip:
    - arrakis-server >= 0.1
