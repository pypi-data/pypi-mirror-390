[pytest]
markers =
    integration: real API tests that hit provider endpoints and may require credentials (LIVE or PAPER);
                 these tests write JSON artifacts under .test_output/ for manual inspection.
log_cli = true
log_cli_level = INFO
