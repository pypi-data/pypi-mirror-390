[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "simulateur-trafic-omar-kassar"
version = "0.1.0"
description = "Simulateur modulaire de réseau routier intelligent"
authors = [{ name="Omar Kassar", email="omarkassar202@gmail.com" }]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "matplotlib",
    "pandas"
]

[tool.setuptools.packages.find]
where = ["simulateur_trafic"]

[project.scripts]
simulateur-trafic = "simulateur_trafic.main:cli"
